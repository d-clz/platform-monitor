apiVersion: v1
kind: Service
metadata:
  name: ci-dashboard
  namespace: platform-cicd
  labels:
    app: ci-dashboard
spec:
  selector:
    app: ci-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: 8080
